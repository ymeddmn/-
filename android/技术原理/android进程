在android中应用程序是一个独立的进程（运行在独立进程可以避免其它应用干扰），当启动一个程序的时候系统会fork一个进程出来，并且为这个进程创建一个主线程应用程序的组件
默认都是运行在主线程中的，我们可以通过   android:process(使用了后再Logcat可以看到同一个应用可以有两个进程)  指定组件运行在一个新的进程中

每一个应用都运行在一个单独的进程中的优点是：
是给每个应用一个内存预算，这样可以给主线程更多的内存预算

